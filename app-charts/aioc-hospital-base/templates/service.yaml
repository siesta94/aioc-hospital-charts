apiVersion: v1
kind: Service
metadata:
  name: {{ include "aioc-hospital-base.fullname" . }}
  labels:
    {{- include "aioc-hospital-base.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aioc-hospital-base.selectorLabels" . | nindent 4 }}
