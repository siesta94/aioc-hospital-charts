apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aioc-hospital-base.fullname" . }}-test-connection"
  labels:
    {{- include "aioc-hospital-base.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aioc-hospital-base.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
